#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int set[3] = {1,2,3};
    int** superset = calloc(sizeof(int), pow(2, 3));

    return 0;
}

void superSet(int** superset, int* set, int len, int start, int end)
{

}
