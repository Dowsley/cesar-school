#include <stdio.h>

int n;   // Qtd. de casos teste.
int m;   // Qtd. de endereços-bases na tabela.
int c;   // Qtd. de chaves que serão inseridas.

struct link
{
	int v;
	struct link *prox;
}temp;

int hash_function(int k, int m)
{

}

int main()
{

	scanf("%d", &n);

	for (n; n > 0; n -= 1)
	{
		scanf("\n%d %d", &m, &c);

		for (m; )
		{

		}

		for (c; c < 0; c -= 1)
		{
			scanf(
				"%d",
				&temp.v
				);
		}
	}

	return 0;
}
